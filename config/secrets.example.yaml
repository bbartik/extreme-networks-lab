# Template for config/secrets.yaml (gitignored). Copy this file to
# config/secrets.yaml and fill in real values before running scripts/push.py.
# The names on the left (SSID names) must match network_policies.yaml
# exactly — push.py looks them up by name.
#
# No site_vars/pos_psk section anymore — BB-Retail-POS moved to PPSK
# (confirmed real, see network_policies.yaml header) instead of one
# literal per-site PSK, so there's no per-site secret to hold here. Real
# per-store PPSK credentials belong in whatever object type provisions
# PPSK identities — not explored this session, not this file.

ssid_psks:
  BB-DC-Ops: "CHANGE_ME"
  BB-DC-IoT: "CHANGE_ME"
  BB-Corp-IoT: "CHANGE_ME"
  BB-PopUp-Staff: "CHANGE_ME"
  BB-PopUp-Guest: "CHANGE_ME"
